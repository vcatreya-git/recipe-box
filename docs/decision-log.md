# Decision Log — Recipe Box

**Standard:** every row must include Made By (agent/person) and Model (concrete model ID or `human`). See `~/.claude/agents/00-Decision-Log-Standard.md`.

| Date | Decision | Reasoning | Made By | Model |
|------|----------|-----------|---------|-------|
| 2026-08-16 | Design v1.6 + Prototype-v2 reviewed — APPROVED WITH CHANGES | Core P0 loops in Prototype-v2.html match the freeze (capture defaults Global, Browse-add personal, globe one-way, generate draft until Shopping list then silent archive, link send, collections, Profile Support). Required patches: S5 still teaches paste-as-primary; Flow 2 receive (open a share link) is undefined after S11 removal; Buy me a coffee is toast-only; leftover S10/S11 still registered. Architecture v1.1 already exists — this is not a start-architecture signal. Full report: [Design-Review-v1.6](design-review.html). | design-reviewer | grok-4.6 |
| 2026-08-16 | Architecture v1.1 NEEDS REWORK — not approved for feature development; hosting stays unlocked | Three product answers (Browse home, fillWeek, extract) match [Design](design.html) v1.6, but the doc is a delta memo, not a build spec. Hard gates fail: P0 API contracts missing (like, collections, cook marks, swap, profile/shape, inbox GET, recipe CRUD, ad-hoc list, share revoke, auth); data model not restated after proto drift; L+T+O below the observability standard; repo `CLAUDE.md` has no Coding Standards; cost is “$0 plus extracts” with no usage assumptions or free-tier limits. Full report: [Architecture-Review-v1.1](architecture-review.html). | architecture-reviewer | grok-4.6 |
| 2026-08-16 | Design v1.6 + Architecture v1.1 refreshed from Prototype-v2; extract uses schema.org then xAI grok-4.5 (no Whisper in V1); Browse home is one payload + client lens; generate is deterministic fillWeek | Owner asked how Discover loads, how the week is filled, and which extract tools run. Must stay cheap: cache URL, markup first, LLM only on miss. Menu is not an LLM. | Grok | grok-4.6 |
| 2026-08-16 | Share is link-only; named-recipient share and Incoming Shares removed from the product surface | Owner: never asked for share-with-people; recipe/list/collection share via generated links; globe is the public path. | Venkat | human |
| 2026-08-16 | Capture confirm defaults Global; Browse-add stays personal and cannot be re-published | Pool grows from new extracts. Republishing a catalogue copy is noise. | Venkat | human |
| 2026-08-16 | Shape is the week: meal count = seat sum; no cycle-to-fill; no weekday packing (Meal 1…N) | A second meal stepper contradicted the buckets. Ten seats is not a calendar. Product owner rejected Mon–Sun packing. | Venkat | human |
| 2026-08-16 | Surprise is a facet/seat; `try_something_new` intent removed; mix-in is a labelled Box-vs-catalogue switch | Same job as “try something new,” but honest. Mix-in is *where*, not *what*. Copy: “Use recipes outside my Box.” | Venkat | human |
| 2026-08-16 | Usual week (onboarding/Profile) seeds seats + people; grocery-saving and mix-in stay This Week only | Standing default vs this week’s mix. Surprises and mix-in must not rewrite identity. | Venkat | human |
| 2026-08-16 | Regenerating archives the prior plan; no generate quiz; no in-week cook nudge; marks are cooked/skip/loved on the meal card | Stale surveys poison “most played.” Optional marks only. Absence is not a skip. | Venkat | human |
| 2026-08-16 | Shopping list: quantities (merged, scaled) + inferred aisle groups + back + share | Dead-end list without amounts was unusable. Aisle is display-time, not a pantry model (delta #4 stands). | Venkat | human |
| 2026-08-16 | Architecture drafted on free-first stack: Vercel + Supabase + PostHog; hosting not locked | Product owner wants $0 start, scale with users, LogRocket-class engagement, GH Actions UT gate. PostHog is the free replay+events equivalent. AWS left as Option B. | Grok | grok-4.6 |
| 2026-08-15 | Skip Hypothesis (Phase 2) and Discovery (Phase 3); go straight to PRD (Phase 5) | Idea is already well-developed — Inbox note has a validated V1 prototype, resolved open questions, and a Fable design refinement pass. User judged further hypothesis/discovery work as redundant for this idea. Phase order is a menu per the workflow, not a mandatory sequence. | human | human |
| 2026-08-15 | Fold in light-touch competitor research (Pepper, ReciMe, Samsung Food, AnyList, Paprika, AI clippers) into agents/discovery-context.md despite Discovery phase being skipped | User pasted real ground research from Grok; too valuable to discard even without running the formal phase. Confirms the Capture+Library+Group gap the product is aimed at, and surfaces a copyright risk on extracted instructions that needs a PRD-time decision. | human (research by Grok) | grok |
| 2026-08-15 | Major model pivot: no persistent group. Replace with independent per-user Recipe Box ("playlist"), a global catalogue, 3-tier visibility (private/specific-users/global), fork-on-receipt (no merge-back), and Playlist vs Station generation modes | User: "there is no group buddy" — corrected the assumed household-group model via a Spotify analogy (catalogue/playlist/station/sharing). Resolves Grok's structural-hole critique more cleanly than either option originally offered, and eliminates the propose-and-merge/flag-error/maker-checker-for-ownership machinery Grok flagged as overbuilt for a small group. | human | human |
| 2026-08-15 | Capture baseline revised: paste-link/screenshot/manual-entry is V1 baseline; mobile share-sheet is an upgrade, not a dependency | Grok flagged iOS PWA share-target limitations and Instagram's unreliable clean-URL handoff as a real technical risk to treating share-sheet as the hero path on day one. | human (research by Grok) | grok |
| 2026-08-15 | "The Spotify of recipes" adopted as the product's core positioning framing | User's own description after the model pivot; clean enough to carry into PRD/design framing directly. | human | human |
| 2026-08-15 | Resolved solution-agent's 3 open questions: "collection" = whole Box for V1 (no sub-playlists); recency-exclusion for multi-participant plans = union-exclude; onboarding seeds a default editable category-slot template | Low-risk defaults already proposed by solution-agent; simplest option in each case, unblocks design-agent immediately. Solution-Sketch.md promoted from draft to confirmed. | human | human |
| 2026-08-15 | Profile taxonomy overhaul: dietary preference splits into single-select diet type + multi-select restriction tags; cuisines become multi-select from an open list, not a fixed dropdown; both dietary tags and cuisines get a "like" vs "want to explore" split | User asked whether dietary/cuisine lists were finite and wanted multi-select, Spotify-onboarding-style. The like/explore split isn't just onboarding polish — it gives the "Try something new" generation intent (added earlier today) a real signal to aim at, instead of guessing randomly from the whole global catalogue. Full curated/swipeable onboarding visual treatment deferred to 7c polish; 7b builds the structural pieces only. | human | human |
| 2026-08-15 | 7b click-through feedback, six changes queued for a solution-agent delta pass: (1) add explicit meal-count control to Generate Plan; (2) replace named-contact selection for "who this generation is for" with headcount + dietary mix — the cook generates regardless of who's eating; sharing a finished plan with specific people stays separate; (3) add cuisine/cook/ingredient filters to Station mode; (4) shopping list must strictly reflect the actual plan's recipes, plus add a lighter ad hoc "select recipes → shopping list" path outside full plan generation; (5) add a required pre-generation intent choice (Balanced / Save on groceries / Try something new) instead of one fixed blended algorithm; (6) remove produce/pantry tagging at recipe-authoring/ingredient level entirely — Pantry Inventory becomes a personal reference list only, shopping list always shows the full ingredient list with pantry-matches flagged (not hidden), plus manual cross-out on any item | User found the named-contact generation input and the produce/pantry ingredient tagging both unintuitive during the 7b click-through, and wants explicit user control over generation intent (optimize vs explore) rather than an implicit algorithm. All six are genuine UX/data-model gaps, not preference bikeshedding. | human | human |
| 2026-08-15 | PRD written at "standard depth" (all 14 sections populated, but Discovery-dependent sections 3–6 honestly flagged as thin evidence — competitor research + single-subject V1 prototype use, no structured primary research) | Matches the template's own guidance for an original idea where Discovery was formally skipped; avoids either fabricating researched-sounding numbers or silently dropping template sections. | prd-agent | claude-sonnet-5 |
| 2026-08-15 | North Star metric and all numeric success targets in PRD Section 12 left as explicit "TBD — no baseline, alpha will establish one" rather than invented figures | No baseline data exists (no primary research, single-subject prototype use only); inventing a number would misrepresent confidence the PRD doesn't have. Alpha phase (Section 10.1) is scoped specifically to produce a real baseline. | prd-agent | claude-sonnet-5 |
| 2026-08-15 | Section 7 diagrams (Solution Layers, User Journey, Hero Flow) embedded as Mermaid source directly in PRD.md rather than generated via the FigJam MCP tool | No FigJam plan key available this session; project workflow explicitly prefers embedded Mermaid (renders natively in Obsidian/GitHub) over a FigJam link when the diagram can be reasonably expressed in Mermaid. | prd-agent | claude-sonnet-5 |
| 2026-08-15 | PRD reviewed by prd-reviewer — verdict APPROVED WITH CHANGES, not a hard block | PRD is internally consistent with the current catalogue/playlist/station model (no stale references to the superseded group model found) and handles the thin evidence base honestly — recommends validation rather than overclaiming. Two items flagged as warnings to resolve before Beta/Launch, not before proceeding to Roadmap: (1) the copyright "resolved" note in Section 4.1 only fully justifies full-text extraction for private/peer-shared recipes — it does not separately re-justify full-text storage for the global-catalogue tier, where any recipe marked global becomes discoverable and copyable by anyone, which is functionally closer to public redistribution than the "personal/peer-shared" framing implies; (2) Section 2.4's claim that AI extraction (schema.org + LLM fallback) is "reliable and cheap enough" for social-video captions/audio (Reels/TikTok) is asserted, not validated — V1 never tested video/audio extraction, and this is P0 MVP scope, so a feasibility spike is recommended during alpha before assuming the mechanism as designed. | prd-reviewer | claude-sonnet-5 |
| 2026-08-15 | Open item added to Section 13.2 candidate list (not yet edited into PRD.md — reviewer does not edit the PRD directly): explicitly track copyright/legal exposure of the global-catalogue tier as a pre-broad-launch open question, distinct from the general "moderation" question already listed | Currently 13.2 lists "global catalogue moderation" as an open question but does not separately flag copyright liability at scale for full-text recipes marked global — these are related but distinct risks and should not be conflated. | prd-reviewer | claude-sonnet-5 |
| 2026-08-15 | Ran a challenge pass (Phase 6.5) specifically against the catalogue/playlist/fork/station model — no blockers found. 7 findings resolved as design decisions or documented risks: (1) global catalogue cold start at alpha/beta scale — mitigate via manual seeding before beta; (2) split `SourceExtraction` (URL-keyed, visibility-agnostic cache) from `Recipe` (per-user, visibility-scoped) so "second clipper gets free extraction" can't leak a prior user's private notes; (3) recency-exclusion for multi-participant generation defaults to union-exclude across participants (open question, needs confirmation); (4) "share a whole playlist/collection" defaults to "collection = entire Box" for V1, no sub-collections (open question, needs confirmation); (5) onboarding needs a seeded default category-slot template or first generation can't fill any slots (PRD delta); (6) complexity-vs-scale noted as an observation only, no change (PRD's P0/P1 split already handles it); (7) forks retain `forked_from_recipe_id`/`source_urls` for traceability even though content is fully independent | Model has zero multi-user validation per the PRD's own admission (Section 4.2) — the solution-agent's mandate is to challenge before designing, specifically on the least field-tested part of the product, before locking the data model that architecture will build from | solution-agent | claude-sonnet-5 |
| 2026-08-15 | Solution sketch locked (entities, 7 P0 flows, 17 API endpoints, integration points, structural risks) — status left as `draft`, pending user confirmation on 3 open questions (collection scope, recency-exclusion scope for multi-participant generation, onboarding default category slots) before promoting to `approved` | Two of the three open questions are genuine schema-shape decisions (collection scope, in particular) that are cheap to confirm now and expensive to change after architecture builds on top of them — per solution-agent's rule to never hand off to architecture with open questions unanswered | solution-agent | claude-sonnet-5 |
| 2026-08-15 | 7a Design.md written directly against confirmed Solution-Sketch.md (no new entities/endpoints introduced) — 20-screen inventory, 8 flows (onboarding + 7 P0 flows), consolidated 19-item edge case table, mandatory User Control Flows section, architecture + engineering handoff notes | Solution-Sketch status is "confirmed" — design's job is to define screens/states/navigation against locked entities and API shape, not re-litigate the model. Onboarding added as its own flow since generation cannot run with zero category slots (Solution-Sketch Challenge 6/Open Question 3, resolved to a seeded default template) | design-agent | claude-sonnet-5 |
| 2026-08-15 | Group-formation/shared-pool-browse screens and share-sheet-as-hero-capture from the earlier Fable pass (design-context.md) dropped entirely rather than reworked | Current model has no persistent group entity and treats mobile OS share-sheet as a P1 fast-path upgrade, not a dependency — there is no version of those screens that fits the confirmed model; brand-personality directions and swap/shopping-list screen thinking from the same Fable pass were retained since they don't depend on the retired group model | design-agent | claude-sonnet-5 |
| 2026-08-15 | FigJam skipped for this design pass; Mermaid flow diagram embedded in Design.md Section 4 instead | Task scope for this session was explicitly limited to the 7a doc and 7b wireframe deliverables; Mermaid is the documented fallback and renders natively in Obsidian, consistent with how PRD Section 7 diagrams were already handled | design-agent | claude-sonnet-5 |
| 2026-08-15 | 7b wireframe built as a single self-contained HTML file (`Prototype-Wireframe.html`) written to the Obsidian project folder, not the git repo, and git was not touched | Matches this project's explicit publishing-cadence rule — builder agents draft in Obsidian only and stop there; repo/GitHub Pages publish happens only on a later explicit "push to GitHub" request | design-agent | claude-sonnet-5 |
| 2026-08-15 | 7b wireframe self-tested via automated checks (embedded JS passed `node --check`; every `showScreen()`/`navTab()` target cross-checked against defined screen IDs, zero orphans) in addition to manual flow tracing, before handoff | Mandatory wireframe self-test step in the design-agent workflow — catching dead buttons/unreachable screens programmatically is more reliable than manual click-through alone for a 20-screen SPA | design-agent | claude-sonnet-5 |
| 2026-08-15 | Design.md (7a) reviewed by design-reviewer — verdict APPROVED WITH CHANGES, not a hard block | All 7 P0 flows from Solution-Sketch.md have real (not hand-waved) screen definitions; confirmed entities/API shape used throughout, no new entities/endpoints introduced; confirmed the superseded persistent-group ("Menon Kitchen") model was actually dropped from the 20-screen inventory, not just claimed dropped in commentary; onboarding category-slot-seeding (Solution-Sketch Challenge 6) is designed as its own screen (S3) with a hard block on zero slots. Two non-blocking issues flagged: (1) S9 Recipe Detail's "Read-only note" state is self-contradictorily worded (says it applies to S13 viewing, not S9, but is listed as an S9 state) — a documentation clarity bug that could confuse an implementing engineer; (2) no explicit "already have this recipe" edge case is defined for the incoming-share accept path (Flow 3/S11), even though the equivalent case is explicitly handled for catalogue adds (S13's "Already in your Box" state) — an inconsistency worth closing for parity. Architecture handoff notes (Section 7) are present and adequate; async/real-time requirements correctly called out as none required beyond deferred push notifications. Prototype (7b wireframe) exists, is self-tested, and is a separate gate the user performs directly — not re-reviewed here. | design-reviewer | claude-sonnet-5 |
| 2026-08-15 | Solution-Sketch.md revised in place via a targeted delta pass (status stays `confirmed`, not re-opened to draft) folding in six 7b wireframe click-through changes: (1) explicit `meal_count` param on Generate Plan; (2) `GenerationInstance` named-contact participants (`participant_user_ids`, `USER }o--o{ GENERATION_INSTANCE`) removed entirely, replaced with plain `headcount`/`dietary_mix` fields — the initiator is the sole owner; sharing a finished plan with specific people is now a separate new entity `PlanShare`, generalizing `RecipeShare`'s pattern (view-only, not fork-on-accept); (3) Station's `GET /api/catalogue` gains `followed_cooks`/`ingredients` filters alongside `cuisine`, promoting `Follow` from P1/lightly-specified to a full V1 entity; (4) `ShoppingList` confirmed strictly derived via full rebuild from a plan's current `PlanDaySlot.recipe_id` set (no drift possible), plus a new ad hoc path (`POST /api/shopping-lists`) building a list directly from selected `Recipe`s with no `GenerationInstance`; (5) required `generation_intent` enum (`balanced`/`save_on_groceries`/`try_something_new`) added to Generate Plan, adding three weighting branches to the existing synchronous slot-fill algorithm alongside the unchanged hard recency-exclude; (6) produce/pantry split removed from `Recipe.ingredients` (now one flat list) and from the extraction schema; `PantryInventoryItem` is now purely a personal reference list matched fuzzily against the shopping list at render time, flagged `pantry_match` (informational, never hides items), with an independent manual `checked` cross-out per item. One new structural risk explicitly flagged (not a blocker): pantry ingredient-name matching is fuzzy/normalized and known-imprecise in V1 — acceptable since the flag is advisory-only. This also mooted the sketch's original Open Question #2 (union-exclude for multi-participant recency) since participants no longer exist; Open Questions #1 and #3 were already resolved prior to this delta and are unaffected. Verified no stale references to removed fields remain anywhere in the document. | Direct user feedback from clicking through the 7b wireframe prototype — named-contact generation input and produce/pantry ingredient tagging were both found unintuitive in hands-on use, and the user wanted explicit control over generation intent rather than one implicit blended algorithm. All six are genuine UX/data-model gaps surfaced by actually using the prototype, not preference bikeshedding — exactly the kind of finding a click-through pass is supposed to catch before architecture locks the schema. | solution-agent | claude-sonnet-5 |
| 2026-08-15 | Solution-Sketch.md revised in place via a second targeted delta pass (status stays `confirmed`) adding a ratings/likes signal for global recipes: new entity `RecipeRating` (id, recipe_id, user_id, value int (V1 fixed at 1 = "liked"), created_at, updated_at (reserved/unused in V1), unique on `recipe_id`+`user_id`) plus a new denormalized `Recipe.like_count` field; new ERD relationships (`USER ||--o{ RECIPE_RATING : rates`, `RECIPE ||--o{ RECIPE_RATING : "rated via"`); new Flow 8 (Rate/Unrate a Global Recipe); two new endpoints (`POST`/`DELETE /api/recipes/:id/like`); `GET /api/catalogue` gains a `sort` param (`sort=popularity` orders by `like_count`) and each result now includes `like_count`/`has_liked`. Modeled as a boolean-like, not a 5-star system, but `value` is an int (not bool) specifically so it extends to a star scale later without a migration. Two judgment calls made and documented inline rather than left open: (a) **who can rate** — gated to users who've added the recipe to their own box (own a `Recipe` with `forked_from_recipe_id` = target), not open to anyone who's merely viewed it, matching the product's "used, not bookmarked" trust bar (same gate philosophy as `CookLog`); rejected "anyone who's viewed it" as too weak/gameable; (b) **retraction** — a simple like/unlike toggle (create/delete row), no confirmation step, no "why did you unlike it" capture. Ratings are strictly per-`Recipe`, never aggregated across a `forked_from_recipe_id` lineage — a published fork always starts at `like_count=0`, same as any new global recipe, per the requirement. Only `visibility=global` recipes are ratable (enforced at the API write path, not a DB constraint, since visibility can change after the fact — historical ratings persist if a recipe later reverts to private). New non-blocking risk flagged: `like_count` is a denormalized counter (not a live `COUNT(RecipeRating)` query) chosen so Station's popularity sort can run on an indexed column; a periodic reconciliation query is the documented recovery path if it ever drifts. No new open questions; no blockers. Verified consistent with existing entities (RLS scoped to owner-only read/write on `RecipeRating` rows, aggregate `like_count` already public via global-recipe readability) and no stale references introduced. | Direct request to model a popularity/quality signal so users can gauge a global recipe before forking a stranger's version into their own box — arrived immediately after the six-change 7b delta pass, confirmed internally consistent, as a seventh isolated change against the same locked sketch. | solution-agent | claude-sonnet-5 |
| 2026-08-15 | Design.md (7a, now v1.1) and Prototype-Wireframe.html (7b) revised in place via a targeted delta pass reflecting all three Solution-Sketch delta passes (headcount/dietary_mix + generation_intent + Station filters/ad-hoc shopping list; ratings/likes). Not a full redo — most of the 20-screen inventory and all 7 P0 flows were left untouched. Changes: (1) Generate Plan (S14) — named-contact participant picker replaced with headcount/dietary-mix steppers, an explicit meal-count stepper (1–7), and a required first step, generation intent (Balanced / Save on groceries / Try something new), Generate blocked until chosen; (2) Station (S12/S13) — added cuisine/followed-cooks/ingredients filters, a popularity sort, `like_count` + a like/unlike toggle gated to users who've already added the recipe to their own Box (shown disabled with a nudge, never hidden), plus a Follow/Unfollow toggle; (3) Shopping List (S17) — produce/pantry split removed in favour of one flat list with a non-hiding pantry-match flag and independent per-item manual cross-out, plus a new ad hoc entry point (Box S8 "Select for shopping list" mode, Flow 6b, no plan required); (4) Confirm/Edit (S7) and Recipe Detail (S9) — produce/pantry tagging removed, ingredients now a single flat list; (5) two design-reviewer documentation fixes — S9's "Read-only note" state reworded to describe S9's own behaviour (was misworded to describe S13's), and S11 gained an "already have this recipe" edge case matching S13's existing parity case; (6) a light addition beyond the six numbered items — S15 gained an outgoing "Share this plan" action (PlanShare, view-only) as the functional replacement for the removed named-participant concept, with the corresponding incoming-accept screen explicitly deferred (documented in Design.md Section 9, not silently dropped). Wireframe re-verified orphan-free after edits: `node --check` passed on the extracted script, zero orphaned `showScreen`/`navTab` targets across 18 full-page screens, all 71 referenced element IDs resolve. Obsidian-only — git/repo untouched, per this project's publish-cadence rule. | Direct user request for a delta rework following 7b click-through feedback, now that Solution-Sketch.md has absorbed three delta passes the design docs hadn't yet caught up to. | design-agent | claude-sonnet-5 |
| 2026-08-15 | Solution-Sketch.md revised in place via a third targeted delta pass (status stays `confirmed`), focused solely on `Profile`'s dietary/cuisine model: `dietary_preference` (singular) + `cuisine_filters[]` replaced with `diet_type` (single-select enum: veg/non_veg/vegan/eggetarian), `dietary_restrictions[]` (multi-select hard-constraint tags — gluten-free/nut-free/dairy-free/etc.), `diet_type_explore[]` (new field, optional multi-select of other diet types the user is willing to explore), and `cuisines_liked_tag_ids[]`/`cuisines_explore_tag_ids[]` (open/multi-select, backed by a new lightweight reference entity `CuisineTag` — system-seeded starter batch + auto-create-on-use, not a hardcoded enum). Required side-effect: `Recipe.cuisine` (free-text) converted to `cuisine_tag_id` (FK → `CuisineTag`) — without a shared vocabulary between Profile's explore set and recipe tagging, cuisines_explore_tag_ids would have nothing to match against. Critical wiring completed, not left as stored-but-ignored: `cuisines_explore_tag_ids` (and `diet_type_explore[]`) now drive the `try_something_new` branch of the Generate Plan slot-fill algorithm (Flow 5) and a new `taste=liked\|explore` param on `GET /api/catalogue` (default `liked`) — `try_something_new` calls the same explore-filter/rank logic internally instead of falling back to an unweighted random pick from the whole global catalogue; `dietary_restrictions[]` remains a hard filter in every mode/intent regardless of `taste`. Two judgment calls made and documented inline rather than left open: (a) `dietary_restrictions[]` gets no explore variant — restrictions are hard constraints/allergens, never safely relaxable, unlike cuisine/diet-type taste preferences; (b) `diet_type_explore[]` was added beyond the originally-listed fields specifically to implement the task's own "willing to explore vegan for a non-vegan" example and to satisfy 00-Project-Context.md's "both dietary tags and cuisines carry a like/explore split" framing. One new non-blocking risk flagged: `CuisineTag` is open/auto-create-on-use, so near-duplicate semantic drift (e.g. "South Indian" vs "Tamil") is possible with no admin dedup tool in V1 — advisory-only, same category as the existing pantry fuzzy-matching risk; mitigated by a starter-batch seed before beta. No blockers; no new open questions. ERD, `Profile`/`Recipe` entity definitions, RLS constraint-check, Flow 4, Flow 5, the `GET /api/catalogue` and `POST /api/plans` API contract rows, seed-risk section, and open-questions section all updated in place; verified no stale references to the removed `dietary_preference`/`cuisine_filters[]`/free-text `Recipe.cuisine` fields remain. | Fourth targeted delta requested directly against the confirmed Solution-Sketch, following 00-Project-Context.md's "Personal profile (revised 2026-08-15)" paragraph which explicitly requires open/extensible cuisine taxonomies, a like/explore split, and a real generation-time signal for "try something new" — the prior model had no mechanism for explore mode to aim at anything but the whole catalogue. | solution-agent | claude-sonnet-5 |
| 2026-08-15 | Solution-Sketch.md revised in place via a fourth targeted delta pass (status stays `confirmed`): the `PantryInventoryItem` entity is removed entirely — no separate reference list, no fuzzy-name auto-matching against the shopping list. `ShoppingList` items drop the `pantry_match` flag; each item now carries only a single manual `checked` boolean, set directly by the user per list instance, with no persistence or matching logic beyond that instance. Removed: `USER \|\|--o{ PANTRY_INVENTORY_ITEM : has` from the ERD, the `PantryInventoryItem` entity block, its mention in the RLS constraint-check list, `pantry_match` from `ShoppingList.items[]`, the pantry-matching step and edge case from Flow 6, and the pantry-matching reference in Flow 6b. No API endpoints existed solely for pantry CRUD (none were ever added to the API contract table), so no endpoint removal was needed there. The prior "pantry-item name matching is fuzzy/known-imprecise" risk (logged in the first 7b delta pass) is explicitly marked resolved/moot in Structural Risks — there is no more matching logic left to be imprecise. Verified no dangling references to `PantryInventoryItem` remain anywhere in the document (remaining mentions are historical delta-pass trace notes describing the removal itself, consistent with how prior deltas preserved history). No blockers; no new open questions. | User found the separate Pantry Inventory reference-list-with-auto-matching concept confusing during click-through, per the updated "Shopping list (revised 2026-08-15, twice...)" paragraph in 00-Project-Context.md (authoritative) — a plain per-item manual checked/struck state is simpler and matches how the feature is actually used. | solution-agent | claude-sonnet-5 |
| 2026-08-15 | Design.md re-review (delta pass #2 onboarding/Box + delta pass #3 image support), combined 7a gate — verdict BLOCKED, not APPROVED WITH CHANGES | Design.md's own written content passes substantively (no entity/field drift from Solution-Sketch; onboarding branching has no dead ends; Box reorg doesn't break "Select for shopping list" mode; image model matches Solution-Sketch delta #5 field-for-field). Block is on `Prototype-Wireframe.html`: `openShareResolution`/`closeShareResolution` (S4 demo buttons — the only current way to test Flow 3/5b accept-a-share) and `setStationTaste`/`toggleStationFilters` (S12) are referenced by onclick handlers but never defined anywhere in the file — three broken buttons across two screens, undetected by the existing self-test (which checks syntax + showScreen/navTab orphans + dangling getElementById refs, but not onclick-vs-defined-function references). Also surfaced an undisclosed divergence: the wireframe's own comments state "No Incoming Shares inbox in this product" (deep-link-only resolution) while Design.md still defines S11 as a persistent screen with a Profile pending-count badge — neither doc nor Decision Log records this change. Required before click-through: fix both function-reference bugs, and either implement working accept/ignore or make dropping S11 an explicit logged decision reflected in Design.md. Required before Phase 8 (not blocking click-through): close Design.md's S2b/S17/S18 **and Flow 6/Flow 6b** documentation gap (confirmed wider than the design-agent's own banner note — Flow 6/6b's step descriptions also still describe the removed pantry-matching mechanism, not just the two screen definitions). Full findings, assumption audit, and an instruction-gap flag (wireframe self-test methodology should add an onclick-vs-defined-function check) recorded in 00-Handoff. | design-reviewer | claude-sonnet-5 |
| 2026-08-15 | Solution-Sketch.md reopened post-confirmation for a fifth targeted delta pass (status stays `confirmed`, scoped surgical fix not a rewrite): added `Recipe.image_url` (nullable, object storage, owner-editable after Confirm, forked as an independent copy not a live reference — same rule as every other forked field) and `SourceExtraction.extracted_image_url` (nullable, cached/reused same as other extracted fields). Flow 1's extraction chain extended to resolve an image per capture path: schema.org `image` property (part of the existing parse step, no new integration); `og:image` meta tag for the non-schema.org LLM-text-fallback path (cheap metadata pull, no AI cost — no image if also missing, not an LLM image-pick); video thumbnail/oEmbed for the social-video path (same cheap-metadata framing); the already-stored screenshot doubles as the image for the screenshot-capture path (no separate extraction). Manual "type name now" entries get no auto-extracted image — flagged as needing a manual-upload affordance at the Confirm screen, queued for design-agent (not designed in this pass). API contracts updated: `POST /api/inbox/capture` (resolves image), `GET /api/inbox/:id` (`draftData` includes `extracted_image_url`), `POST /api/recipes/:inboxId/confirm` (`image_url` request field, response includes it). Existing "Object storage" Integration Points row broadened to cover recipe display images generally, not a new integration. New Structural Risks note: LLM/vision-based "pick the best image from the page" explicitly out of scope for V1 (metadata-only resolution), flagged so it isn't silently re-scoped later. No other entity, flow, or endpoint touched — fork model, visibility rules, and generation logic untouched. Design.md/Prototype-Wireframe.html/PRD.md explicitly not touched in this pass; S7/S8/S9/S12/S13 Design.md updates queued as separate follow-up work (00-Status.md). | Direct inspection found neither `Recipe` nor `SourceExtraction` had any image field, despite PRD.md's own design principles (line 396) already assuming every recipe card shows an image/thumbnail — a data-model/PRD mismatch. Product owner confirmed the gap directly and confirmed images are fundamentally important to this product, requesting a proper fix rather than a workaround. | solution-agent | claude-sonnet-5 |
| 2026-08-16 | PRD.md revised in place: mobile OS share-sheet capture promoted from P1 fast-path to **P0, the primary capture path**, replacing paste-link/screenshot/manual entry as the app's main way to get a recipe in (those remain as fallback entry points, not primary). Section 2 MVP Scope, Section 7.2 Feature Breakdown, and Section 13.2 Open Questions updated. Platform reality documented honestly: Android gets a true native share-target via the PWA Web Share Target API (manifest `share_target` entry, no native app shell needed); iOS cannot register a plain installed web app as a Share Sheet target, so the V1 answer is a distributable iOS Shortcut (one-time user setup, POSTs the shared URL to the existing capture endpoint) rather than a true zero-setup native Share Extension. New open question logged: iOS Shortcut install/completion rate is unvalidated and should be an explicit alpha metric. | Product owner rejected copy-paste-link as an acceptable primary capture mechanism twice, directly ("I myself won't copy paste links into this, too much of friction") — this is a hard product requirement, not a preference. The prior framing (share-sheet is P1 because it's "real native engineering") was corrected: Android's cost was overstated (PWA Web Share Target is cheap, not native-app-level work); iOS genuinely is harder without a native wrapper, which is why the two platforms get different V1 mechanisms rather than a false parity claim. Solution-Sketch.md's corresponding integration-point addition (share_target intake route, iOS Shortcut distribution) and Design.md/S5 Capture Entry updates are queued as separate follow-up passes, not done in this edit. | claude (orchestrator) | claude-sonnet-5 |
| 2026-08-16 | Solution-Sketch.md reopened post-confirmation for a sixth targeted delta pass (status stays `confirmed`, scoped surgical fix not a rewrite): added lightweight Collections. New entity `Collection` (id, owner_user_id FK, name, created_at, updated_at) — private, user-named grouping, strictly owner-scoped, never shared/visible to any other user regardless of member recipes' own `visibility`; no rename endpoint in V1 (delete-and-recreate only). New join entity `RecipeCollection` (recipe_id FK → Recipe, collection_id FK → Collection, added_at, unique on `(recipe_id, collection_id)`) — many-to-many, additive to and independent of `Recipe.cuisine_tag_id`; membership requires the caller to own the target `Recipe` directly. New endpoints: `POST`/`GET /api/collections`, `POST /api/recipes/:id/collections` (accepts `collection_id` or `name`, the latter creating a `Collection` inline), `DELETE /api/recipes/:id/collections/:collectionId`, and `DELETE /api/collections/:id` (added beyond the four named endpoints, needed to give the entity a concrete deletion path). `GET /api/recipes` (Box listing) formalized as an explicit contract row for the first time, gaining an optional `collection_id` filter. New Flow 9 (Organize Recipes into Collections) traced end-to-end. Deleting a `Collection` hard-deletes all its `RecipeCollection` rows (no orphan risk — a collection is a label, referenced by no other entity); deleting a `Recipe` cascades the same way. Structural Risks gained an RLS note (strict two-sided ownership check), a computation-risk note (`recipe_count` computed query-time, deliberately not denormalized, contrasted against `Recipe.like_count`'s denormalized-counter choice), and a seed/bootstrap note (empty Collections is a normal state, no cold-start problem). Terminology flag, not a reversal: Open Question #1's earlier "collection = whole Box" resolution was specifically about *sharing* the whole Box as one unit and remains untouched — this delta's `Collection` entity is an unrelated, newly introduced, purely personal-organization construct that happens to reuse the same word, called out explicitly so the two aren't conflated by a future reader. No other entity, flow, or endpoint touched — fork-on-receipt model, visibility rules, generation logic, and `CategorySlotDefinition` untouched; no new integration/service. Design.md/`Prototype-Full.html`/`Prototype-Wireframe.html`/PRD.md explicitly not touched in this pass; Design.md S8/S9 updates queued as separate follow-up work, with a separate agent handling `Prototype-Full.html` in parallel against this same spec (00-Status.md). | Product owner clicked through the 7c prototype and flagged that Box's only organization is automatic `cuisine_tag_id` grouping — no user-created organization exists. PRD.md's Persona 2 ("Recipe Collector," Section 5.1) explicitly lists "bookmark graveyard" as the first of its top 3 pain points; a user-named way to group recipes across cuisines directly serves that gap in a way automatic cuisine grouping alone cannot. Recommended and approved: add lightweight Collections, deliberately minimal — no nesting, no sharing/visibility on collections, no reordering semantics beyond simple membership. | solution-agent | claude-sonnet-5 |
| 2026-08-16 | Solution-Sketch.md reopened post-confirmation for a seventh targeted delta pass (status stays `confirmed`, scoped surgical fix, entry points only — no new entities/fields/endpoints): implements the already-made 2026-08-16 PRD delta (row above) promoting mobile OS share-sheet capture to P0/primary. Flow 1's Trigger and step 1 rewritten so share-sheet leads: Android's OS share action hands off to the PWA's registered `share_target` route, which calls the unchanged `POST /api/inbox/capture` with `{type:"url", url}`; iOS's installed Shortcut performs the equivalent raw POST to the same endpoint. Paste-link/screenshot/manual entry unchanged in behavior, relabeled fallback. Confirmed explicitly (not assumed) that `POST /api/inbox/capture`'s existing `{type, url?, image?, name?}` shape needs no change across all three entry points. Judgment call, decided against by default: rejected adding a `source` field to the capture contract to distinguish entry-point origin — judged a client-side product-telemetry concern, not a data-model concern, and scope creep on the one endpoint every entry point depends on. Two new Integration Points rows (Android Web Share Target — manifest config + intake route, standard PWA capability, no third-party service; iOS Shortcut distribution — static hosted asset, not a runtime service). New Local-Dev Constraints note: Android share-target registration generally can't be exercised from plain `localhost` (needs an installed/served PWA context), while the underlying capture endpoint stays fully testable locally regardless; iOS Shortcut testing has no such constraint. New Structural Risks category ("Capture entry-point prerequisites"): both platforms require a one-time setup step (PWA install on Android, Shortcut install on iOS) before the primary path works — the prompting/copy UX for both is explicitly deferred to Design.md (S1 onboarding / S5 Capture Entry), not decided here. New edge case on Flow 1: unauthenticated/expired-session capture via share-sheet/Shortcut can't interactively redirect to login the way the in-app flow can — same 401 at the API level, failure-surfacing UX deferred to design-agent. No blockers; no new open questions. | Product owner rejected copy-paste-link as workable primary capture UX, directly, twice ("I myself won't copy paste links into this, too much of friction") — already decided and implemented in PRD.md by the orchestrator; this pass makes Solution-Sketch.md match that decision, not re-derive it. | solution-agent | claude-sonnet-5 |
| 2026-08-16 | Solution-Sketch.md reopened post-confirmation for an eighth targeted delta pass (status stays `confirmed`, scoped surgical addition, not a rewrite): two complementary, orthogonal trust/safety mechanisms, both fully spec'd via product-owner/Grok-refinement discussion — implemented as given, not re-derived. **Part 1 — extraction confidence:** wires up the previously-unused `SourceExtraction.raw_confidence` as a deterministic field-checklist score (real title; ≥4 non-filler ingredient lines; instructions past a trivial one-liner; enough source material returned) rather than a raw self-reported LLM confidence value — schema.org starts higher by construction, LLM/OCR paths start lower; any self-reported LLM signal is a tie-break only, never primary, documented as a deliberate rejection of the poorly-calibrated "ask the LLM how sure it is" approach. New `SourceExtraction.confidence_reason` (nullable, extensible enum) names why a score is low. New `Recipe.needs_care` (boolean) defaults from the source extraction's low-confidence flag at Confirm; cleared only when the user edits **and saves** `ingredients` and/or `instructions` specifically — no other field edit clears it (a title fix does not). Confirm/Edit (S7) gets a non-blocking banner naming `confidence_reason` when `raw_confidence` is below a cutoff — **the cutoff itself is explicitly not hardcoded**, logged as a genuine open question requiring empirical calibration against real alpha extraction data (a naive fixed threshold risks flagging most video-sourced extractions by default, training users to ignore the banner). Global-visibility checkpoint (S10 / Flow 2): a still-`needs_care=true` recipe shows the flag again with a one-tap "Publish anyway" path (never a hard block); publishing anyway writes new `Recipe.user_confirmed_low_confidence=true` — a self-reported signal, not a moderation action. That signal is wired into `GET /api/catalogue`'s `sort=popularity`/Trending/Popular ranking to deprioritize such recipes by default — closing the "stored but unused" gap for `raw_confidence`. New/updated API contract rows: `GET /api/inbox/:id`, `POST /api/recipes/:inboxId/confirm`, new `PATCH /api/recipes/:id` (formalized for the first time), `PATCH /api/recipes/:id/visibility`, `GET /api/catalogue`. **Part 2 — rate limiting + manual block (orthogonal, unrelated data model):** billable-action-only rate limiting (paste-link, screenshot OCR, share-sheet-triggered extraction, explicit re-extraction — explicitly not manual name-only entry, editing, generating a plan, or browsing) via a per-user, never per-IP, rolling window checked before extraction runs (new Flow 1 step 0) so a rejection never incurs LLM/OCR cost; window numbers left tunable/deferred to architecture. Rejection response on `POST /api/inbox/capture` always includes current count + reset time, same message regardless of cause (volume-based protection doesn't need intent). New `User.blocked` (boolean, default false) — a manual kill switch, not automated detection — checked at `POST /api/inbox/capture`, `PATCH /api/recipes/:id/visibility` (Global), `POST /api/recipes/:id/share`, and `POST /api/plans/:id/share`; all other surfaces (Box, cooking/Recipe Detail, This Week/Generate, Shopping List, Profile) remain fully usable when blocked — a narrow, deliberate scope, not account suspension. No admin UI, no auto-detection, no moderation queue in either part — confirmed consistent with, and complementary to, PRD 13.2's existing "no moderation queue for V1" decision (not reopened). Since there's no admin surface, flipping `User.blocked` in V1 is documented as a concrete direct-database-write runbook step, not left as an abstract flag. No fork-on-receipt, visibility-tier, or generation-logic changes. PRD.md, Design.md, `Prototype-Full.html`, `Prototype-Wireframe.html`, `Prototype.html` explicitly not touched. | Both mechanisms were fully specced through direct product-owner discussion (refined with Grok) before this pass began — the task was to implement the already-agreed shape into the sketch's entities/flows/API contracts/structural risks, not to re-derive or challenge the design. Deliberately kept additive and scoped: confirmed no automated abuse detection, no moderation queue, no second confidence-scoring model, and no changes to the fork-on-receipt model, visibility tiers, or generation logic were introduced. | solution-agent | claude-sonnet-5 |
| 2026-08-16 | Solution-Sketch.md reopened post-confirmation for a ninth targeted delta pass (status stays `confirmed`, scoped correction, `PlanShare`-only): `PlanShare` redefined from an internal, named-recipient, accept/ignore mechanism (7b delta change 2) to a link-based share — a generated, unguessable `share_token` per plan (`id, plan_id, share_token, created_at, revoked_at`), no `recipient_user_id`, no `recipient_contact_identifier`, no `status`/accept-ignore lifecycle. New/changed endpoints: `POST /api/plans/:id/share` (owner-only, creates-or-returns the plan's active share + shareable URL, idempotent, no body), `GET /api/shared-plan/:token` (**public, unauthenticated**, read-only plan + shopping-list view, gated only by a valid non-revoked token), `DELETE /api/plans/:id/share` (owner-only, revokes by setting `revoked_at`). `GET /api/plan-shares/incoming`, `POST /api/plan-shares/:id/accept`, and `POST /api/plan-shares/:id/ignore` removed entirely. Structural Risks' RLS section documents the new public route as a deliberate, narrow exception to the sketch's otherwise-universal owner/participant-scoped access rule — explicitly not a general precedent for other data. Deliberately asymmetric with `RecipeShare` (untouched): a shared Recipe is a permanent, owned, forkable asset worth an account + accept flow + fork-on-receipt; a shared Plan is ephemeral/informational, so a lightweight unauthenticated link fits it better. Flagged for a follow-up Design.md pass (not done here): the previously deferred "Incoming Plan-Shares screen" (Design.md Out of Scope, mirroring `RecipeShare`'s S11) no longer exists under this model and should be removed from that list, not built. No other entity, flow, or endpoint touched — `RecipeShare`, fork-on-receipt, visibility tiers, and generation logic untouched. PRD.md, Design.md, `Prototype-Full.html`, `Prototype-Wireframe.html`, `Prototype.html` explicitly not touched — a parallel design-agent pass is updating `Prototype-Full.html`'s plan-share UI to a copy-link/send-via-WhatsApp-style panel against this same spec. | Product owner tried the prototype's "Share this plan" action directly and rejected the internal named-recipient/accept-ignore model — direct feedback that plan sharing should be a plain shareable link (WhatsApp/iMessage/email/etc.), viewable without a Recipe Box account, since a plan is ephemeral/informational and nobody owns or forks it, unlike a shared Recipe. | solution-agent | claude-sonnet-5 |
| 2026-08-16 | Solution-Sketch.md reopened post-confirmation for a tenth targeted delta pass (status stays `confirmed`, scoped surgical addition, not a rewrite): two related, fully additive changes to `SourceExtraction`/`Recipe`, no new entities. **Part 1 — creator attribution:** new nullable `SourceExtraction.creator_name`/`Recipe.creator_name`, resolved per capture path exactly like `extracted_image_url` (schema.org `author`; `<meta name="author">` for the non-schema.org path; oEmbed `author_name` for social video; null for screenshot/manual), copied to `Recipe` at Confirm, owner-editable after, forked as an independent snapshot. `GET /api/catalogue`'s `query` search now also matches `creator_name`. Dish-grouped search (e.g. "Sambar — 3 versions") — a pre-existing product behaviour shipped in `Prototype-Full.html`'s 7c build but never formalized in this sketch — is formalized in Flow 4 to the depth needed to require: per-version `creator_name` display, wired explicitly into the response/display contract (not left stored-but-unused, the exact `raw_confidence` mistake delta pass #8 existed to fix); and that a dish group always returns every matching version, never a truncated subset. **Part 2 — engagement signal, explicitly bias-aware:** new `Recipe.box_add_count` (denormalized, one-way, never decremented — a historical adoption count), incrementing only on organic-discovery adoption (`POST /api/recipes/:id/add-to-box`, swap-in-from-Browse fork creation — the latter itself a minimal, narrowly-scoped documentation catch-up to Flow 5, since swap-in-from-Browse was also a pre-existing unformalized prototype behaviour), never on accepting a targeted `RecipeShare` — chosen over raw share-action tracking for two reasons: privacy (share-tracking would expose who shared with whom) and signal quality (one recipe forwarded to fifty people vs. fifty independent adoptions look identical as share-events but are very different signals; adoption count is the one that distinguishes them). New nullable `SourceExtraction`/`Recipe` fields `external_rating_value`/`external_rating_count` (schema.org `aggregateRating`) and `external_engagement_count` (video/oEmbed view-or-like count) captured cheaply at extraction time to fight cold-start, stored raw and separate per source type — explicitly **not** blended into one fabricated normalized cross-platform score, and explicitly **not** owner-editable (unlike `creator_name`/`image_url`), since a user editing them could fabricate external validation. `GET /api/catalogue`'s dish-grouped/Popular/Trending ranking gains a new explicit **requirement** (not a formula, exact weighting deferred to architecture/tuning against real alpha data, flagged as a genuine open question): cumulative in-app counters alone must never be the sole sort key; external signal must let a low-in-app-engagement recipe rank competitively; and some new-item exposure-fairness mechanism must exist. New "Engagement ranking / cold-start bias mitigation" Structural Risks category documents the failure mode, both requirements, and why the weighting is deliberately left uncalibrated. No `Creator` entity, no follow/verification mechanism, no per-share-action tracking, no fabricated normalized external score — all explicitly rejected, matching the product owner's own stated scoping. No other entity, flow, fork model, visibility tier, or generation-logic piece touched. PRD.md, Design.md, `Prototype-Full.html`, `Prototype-Wireframe.html`, `Prototype.html` explicitly not touched — Design.md's S12/S7/S9 will need a follow-up pass once the already-queued Station/Browse rewrite happens, not done here. | Product owner's direct example (Sanjyot Kher's Pav Bhaji vs. Ranveer Brar's Paneer Lababdar needing to read as distinguishable dish-grouped-search entries) plus a direct, explicit concern about pure in-app-engagement ranking being rich-get-richer/biased against a genuinely excellent but newly-captured recipe — both raised by the product owner directly, requiring a data-model fix (Part 1) and a deliberately bias-aware ranking requirement rather than a naive popularity sort (Part 2). | solution-agent | claude-sonnet-5 |
| 2026-08-16 | `Prototype-Full.html` (7c) large consolidated pass, 9 changes in one coherent build, driven by direct product-owner feedback plus a structured critique (a competing prototype's simulated user walkthrough) that converged on the same problems independently: (1) navigation collapsed from 5 tabs + a hidden logo-only Dashboard + FAB to 4 tabs (Browse · My Box · This Week · Profile) + FAB — no hidden home screen; Shopping List is now reached from This Week (a button), not its own tab; the old Dashboard's cook-ritual prompt/idea-pool/quick-links are folded into a "for you today" strip at the top of My Box, rendered only when there's something to show; (2) "Station" renamed "Browse" in all user-facing copy, "Playlist" dropped from user-facing copy entirely (internal ids/function names deliberately left alone — copy-only rename); (3) liking un-gated — any authenticated user can like/unlike any `visibility=global` recipe directly, no box-ownership prerequisite (reverses the `RecipeRating` "who can rate" UX gate; the underlying like_count/has_liked plumbing is untouched); (4) "Follow a cook" removed entirely — no Follow/Unfollow control anywhere, no followed-cooks rail/filter, Browse's three-rail layout (Trending/Popular/Followed) is now two rails (Trending/Popular); (5) Generate rebuilt defaults-first — the old 4-step gate (intent + headcount + meal count + Playlist-vs-Station mode, all required before Generate would run) is gone; Generate now always sources from the user's own Box by default (never silently mixing in unverified global recipes) with sensible defaults (balanced intent, household size from Profile or 2, a full 7-day week) and produces a week on one tap; intent/headcount/meal-count/an explicit opt-in "mix in a few Browse picks" toggle live in a collapsed Adjust panel, reachable both pre-generate and via a new "Adjust & regenerate" action on the Plan view; (6) onboarding's cuisine-preference question replaced with a Spotify-artist-picker-style grid of ~26 (sampled from a curated 48) tappable named-dish tiles with a soft locale bias (`navigator.language`/`Intl` timezone) and no new onboarding question added; selecting 3+ tiles (mirrors the existing "at least one" pattern) populates `cuisines_liked_tag_ids[]` from the tiles' cuisine tags; the separate "cuisines/diet you want to explore" question is dropped from onboarding entirely — `cuisines_explore_tag_ids[]`/`diet_type_explore[]` stay empty/unset until set later via Profile & Settings or organically via Browse's Discover lens (a one-line nudge added, not a full inference UI, per the task's own "trivial nice-to-have" allowance); dietary restrictions stay their own explicit, direct question (safety-relevant, never inferred from dish taps); (7) visibility badges now only render for Shared/Global — Private (the default) shows no badge anywhere; (8) added a 1–5 "Serves" stepper to Recipe Detail's ingredients that proportionally scales parseable leading amounts (integers, decimals, ascii/unicode fractions, mixed numbers) and leaves freeform amounts (e.g. "a lime-sized ball," "salt to taste") completely unscaled with a "doesn't scale" tag, never guessed — required adding a `servings` base-count field to the prototype's Recipe shape (default 4), not currently in Solution-Sketch's `Recipe` entity, flagged as a follow-up delta, not made official here; a generated plan's shopping list now scales each recipe's contribution off the plan's own headcount by default (documented judgment call — the per-recipe stepper is a display-only override, doesn't feed back into the list); (9) added a free-text search input to My Box (same placement/spirit as Browse's), working alongside the existing filter chips and cuisine/collection grouping. Self-tested beyond the project's standard bar: `node --check`, zero orphaned nav targets (17 screens), zero dangling `getElementById` refs, zero onclick/onchange/oninput-vs-defined-function misses (94 distinct call names), plus a full jsdom real-DOM pass (68 assertions) covering all 4 tabs + capture + recipe detail navigation, the dish-tile picker end-to-end, Generate producing an immediate ungated week, Adjust expand/collapse, un-gated liking, zero remaining Follow UI/state/functions, serving-size scaling on 2+ parseable formats plus 1 freeform-unscaled case, and My Box search — all pass (one test-script-only false positive, confirmed by direct grep, not an app bug). Design.md/Solution-Sketch.md/PRD.md were **not** updated to match (deliberately deferred per instruction) — drift on nav model, Station→Browse naming, the RecipeRating like-gate reversal, Follow's now-dead entity/filter, Generate's defaults-first redesign, onboarding's dish-tile mechanism, and the new `servings` field is flagged in `00-Handoff.md` for a follow-up reconciliation pass, not fixed here. | Direct product-owner feedback ("I don't know where dinner lives," Shopping List empty most of the week doesn't deserve a permanent tab, "I already follow them on Instagram," a stranger's untested recipe shouldn't land on a real day uninvited by default) converged with an independent structured critique (a competing prototype's simulated user walkthrough) on the same core problems from two directions — treated as one coherent redesign pass rather than unrelated tweaks, per the task's own framing. Product owner explicitly wants the prototype frozen after this pass pending their own review; documentation reconciliation is deliberately deferred, not skipped. | design-agent | claude-sonnet-5 |
| 2026-08-16 | Solution-Sketch.md reopened post-confirmation for an eleventh targeted delta pass (status stays `confirmed`, scoped surgical addition, no new entity): closes a gap where `Recipe.cuisine_tag_id`/`dietary_tags[]`/`effort`/`cook_time_minutes` — unlike every other extractable Flow 1 field (title, ingredients, instructions, image, creator name, external ratings) — were never wired into the extraction pipeline at all and started blank on every capture. Added four new nullable `SourceExtraction` fields — `suggested_cuisine_tag_id` (FK → CuisineTag), `suggested_dietary_tags[]` (same vocabulary as `Profile.dietary_restrictions[]`), `suggested_effort` (same vocabulary as `Recipe.effort`), `suggested_cook_time_minutes` (int) — resolved per capture path in Flow 1 with the same "cheap metadata pull, no new AI cost" framing as delta pass #5/#10: schema.org `recipeCuisine`/`recipeCategory` (normalized against `CuisineTag`'s existing auto-create-on-use vocabulary) and `cookTime`/`totalTime`/`prepTime` for the schema.org path (no `suggested_effort` on this path, no clean mapping exists); the one genuinely new (small) cost in this pass — the already-happening LLM call extracting ingredients/instructions on the non-schema.org/social-video paths is extended to also return suggested cuisine, effort, and cook-time, a marginal addition to an existing call, not a new call; dietary tags inferred deterministically (no LLM call) from the already-extracted ingredient list on any path where ingredients exist, including screenshot-with-successful-OCR. The four suggestions pre-fill `Recipe.cuisine_tag_id`/`dietary_tags[]`/`effort`/`cook_time_minutes` as editable defaults at Confirm (S7), same UX pattern as every other pre-filled field; staying blank if nothing was resolved, exactly as today, on manual entry or screenshot-with-failed-OCR. `POST /api/inbox/capture`, `GET /api/inbox/:id`, and `POST /api/recipes/:inboxId/confirm` contract rows updated to include the four fields. New edge case in Flow 1 documenting the no-signal-resolved case as unchanged baseline behaviour. Critical distinction documented inline in both `SourceExtraction` and `Recipe` entity definitions, not just the banner: `suggested_dietary_tags[]`/`Recipe.dietary_tags[]` are corrigible tagging *suggestions*, structurally unrelated to and never read by `Profile.dietary_restrictions[]`'s role as a hard, always-enforced allergen/safety filter (delta pass #3, untouched) — nothing safety-critical rests on the suggestion being correct. New documented, low-stakes imprecision risk: the ingredient-keyword heuristic behind dietary-tag inference is necessarily approximate (same risk category as `CuisineTag` near-duplicate drift, delta #3) — exact keyword list left to architecture, not specified here. No other entity, flow, fork model, visibility tier, or generation-logic piece touched; `CuisineTag`'s existing mechanism reused as-is. PRD.md, Design.md, `Prototype-Full.html`, `Prototype-Wireframe.html`, `Prototype.html` explicitly not touched — a separate design-agent seed-data-expansion pass on `Prototype-Full.html` may still be in flight and was not interfered with. Design.md's S7 (Confirm/Edit) and `Prototype-Full.html`'s Confirm screen both need a follow-up pass to add and pre-fill the four fields as editable suggestions — flagged, not done here. | Product owner, direct: every other extractable field in Flow 1 follows an "extract what's cheap, suggest it, let the user correct it" pattern, but cuisine/dietary-tags/effort/cook-time were the one category missed entirely, requiring full manual tagging on every single capture — unintentional friction, not a scoping choice, and should be fixed to match the established pattern exactly. | solution-agent | claude-sonnet-5 |
| 2026-08-16 | Solution-Sketch.md reopened post-confirmation for a twelfth targeted delta pass (status stays `confirmed`, two related taxonomy-granularity fixes folded into one pass): **Part 1** — `Profile.diet_type` (single-select, delta pass #3) split into two independent multi-select axes: `diet_animal_stance[]` (open/extensible, starter set vegetarian/non_vegetarian/vegan/eggetarian/pescatarian) and `diet_frameworks[]` (open/extensible, starter set paleo/keto/mediterranean/low_carb/whole30/halal/kosher/flexitarian/high_protein) — the enum conflated animal-product stance with diet framework/style, two signals that combine freely (non-veg + keto) and were wrongly forced into one mutually-exclusive value; separately, single-select `diet_type` also mismodeled `non_vegetarian` as exclusive when it should be inclusive (a non-vegetarian eater still eats vegetarian meals, so selecting it must never hide vegetarian recipes) while `vegetarian`/`vegan` genuinely are exclusionary — an asymmetry no single mutually-exclusive value can express. `diet_type_explore[]` (delta pass #3) split identically into `diet_animal_stance_explore[]`/`diet_frameworks_explore[]`, closing the instruction not to leave an asymmetric explore model. Filtering behavior, asymmetric by design and stated explicitly: `vegetarian`/`vegan` hard-exclude at the same strength as `dietary_restrictions[]` (vegan a superset exclusion); `pescatarian`/`eggetarian` apply their own inclusion rule; `non_vegetarian` is inclusive and, if selected alongside any restrictive value, makes the whole `diet_animal_stance[]` axis permissive (union/OR semantics across the multi-select, not AND); `diet_frameworks[]`/`diet_frameworks_explore[]` are weighting-only, never a hard exclude, combining freely with any animal-stance selection. `dietary_restrictions[]` untouched, remains the sole hard, always-enforced, highest-priority filter. Judgment call: neither new field gets a `CuisineTag`-style backing reference table, since neither has a symmetric `Recipe`-side FK column requiring referential matching — filtering reuses the existing `dietary_restrictions[]`/`Recipe.dietary_tags[]` matching mechanism, not a new one. Every live reference updated (Profile entity, Flow 4, Flow 5, `GET /api/catalogue` and `POST /api/plans` contract rows, RLS/constraint-check, Computation/Seed-bootstrap risks, an onboarding UX-implication note flagged for design-agent, not designed here); historical delta-pass banners describing the superseded `diet_type` model left as-is, per this sketch's established change-history convention. **Part 2** — `CuisineTag` (flat, delta pass #3) gains a two-level parent/child hierarchy: new `parent_cuisine_tag_id` (nullable, self-referential FK → `CuisineTag.id`, null = top-level parent, set = one-level-nested child) — two levels only, enforced at the application layer, though the self-referential FK naturally supports more depth later if ever needed. Fixes the same underlying granularity gap as the already-documented near-duplicate-drift risk (e.g. "Tamil" vs. "South Indian") — product owner's own examples: "Chinese" collapsing Szechuan/Hakka/Cantonese, "South Indian" collapsing Chettinad/Andhra/Malabar/Udupi. `Recipe.cuisine_tag_id` can point to either level; auto-create-on-use defaults a new tag to top-level/parent unless explicitly created as a sub-cuisine (no forced parent-classification friction on the most common taxonomy action); re-parenting an existing tag after creation is named as a future curation direction with no endpoint built in this pass, consistent with the explicit exclusion of admin curation tooling. New explicit `GET /api/catalogue` requirement: filtering by a parent aggregates its children; filtering by a specific child returns only that child. Starter-batch seeding requirement extended (described, not seed SQL) to include parent+child cuisines, at minimum Chinese and South/North Indian regional styles. Honesty note, not an elimination claim, added to the existing near-duplicate-drift risk bullet: this mitigates but doesn't eliminate the risk, since a user can still create a near-duplicate parent instead of nesting correctly, with no admin merge/dedup tool in V1. ERD, `CuisineTag`/`Recipe`/`SourceExtraction` entity references, RLS constraint-check, and Structural Risks all updated in place. **Explicitly out of scope for both parts:** deeper-than-two-level nesting; admin curation tooling (merge/rename/re-parent UI); any change to `dietary_restrictions[]`'s role, fork model, visibility, or generation logic beyond the described filtering-step updates. Confirmed zero stale `diet_type` references remain outside intentional historical banners and this pass's own explanatory text; confirmed `diet_type_explore[]` did exist and received the identical split treatment. PRD.md, Design.md, `Prototype-Full.html`, `Prototype-Wireframe.html`, `Prototype.html` explicitly not touched — the prior seed-data-expansion pass on `Prototype-Full.html` had already anticipated this delta pass by name and is now complete, so `Prototype-Full.html`'s follow-up work is queued next, not started here. | Product owner, direct, two related taxonomy-granularity corrections given together: (1) single-select `diet_type` conflated animal-product stance with diet framework/style and mismodeled non-vegetarian as exclusive when a non-vegetarian eater regularly eats vegetarian meals too; (2) flat `CuisineTag` values like "Chinese"/"South Indian" are too broad to represent meaningfully distinct regional/sub-cuisine styles (Szechuan/Hakka/Cantonese; Chettinad/Andhra/Malabar/Udupi), the same underlying cause as the already-documented near-duplicate-drift risk. | solution-agent | claude-sonnet-5 |
| 2026-08-16 | Solution-Sketch delta #14 + Design.md v1.4 close Status (a)–(p) in one pass. Sketch makes official: flexible buckets on `CategorySlotDefinition`; `Follow` deleted from V1; likes ungated on global recipes; `mix_in_browse` replaces `GenerationInstance.mode`; `Recipe.servings` added. `Profile.tasteSignals` left open, not invented. Design.md rewritten to the current 7c model (4-tab Browse/Box/This Week/Profile, no Dashboard/Pantry, share-sheet-primary, taste-graph with resume-on-back, empty Box after onboarding, Collections, inline vis, link PlanShare, no Incoming Plan-Shares, no dietary_mix, dish/version/creator, tag suggestions, diet axes, parent/child cuisine). Prototype-v2 gets the remaining (o) cuisine tree. `Prototype-Full.html` untouched. design-reviewer not invoked. Git not touched. | Formalizes already-shipped 7c prototype decisions the Status queue had listed as (a)–(p). Product owner ordered "go a to p first" after off-queue proto work treated queued items as invented holes. | Grok | grok-4.6 |
| 2026-08-16 | Solution-Sketch.md reopened post-confirmation for a thirteenth targeted delta pass (status stays `confirmed`, scoped simplification, `GenerationInstance`-only): `dietary_mix` (the `{vegetarian: 1, none: 3}`-style per-headcount dietary breakdown introduced at 7b delta changes 1/2/5) is removed entirely from `GenerationInstance`'s field list — no replacement structure, no per-plan dietary override of any kind. Generate's slot-fill filtering (Flow 5, step 3) now reads dietary constraints exclusively from the plan initiator's own `Profile`: `dietary_restrictions[]` (hard, delta pass #3), `diet_animal_stance[]` (hard via its union/OR semantics, delta pass #12), and `diet_frameworks[]` (weighting-only, delta pass #12) — no per-plan/per-person breakdown at generation time. `headcount` is kept on `GenerationInstance` — still useful, feeds serving-size/shopping-list quantity scaling — but is now explicitly decoupled from dietary structure: a plain integer, nothing more. `POST /api/plans`'s request shape loses `dietary_mix`; the `GenerationInstance` and `PlanShare` entity definitions, Flow 5's trigger/step 1/step 3, the `POST /api/plans` API contract row, and the RLS/constraint-check section are all updated in place. Confirmed zero stale `dietary_mix` references remain outside intentional historical banners (the 7b delta banner that originally introduced it, and this pass's own explanatory text) — the sketch's established convention of preserving change history rather than retroactively rewriting prior banners. Judgment call, documented inline rather than left open: `dietary_mix` is removed from the field list entirely rather than kept as a nullable/unused field, since Solution-Sketch has never shipped (no migration/backfill risk, same reasoning already applied to the `Recipe.cuisine` free-text conversion at delta pass #3 and the `diet_type` split at delta pass #12) and carrying a dead field forward would misdescribe the entity. **Explicitly out of scope, per this pass's own boundary:** `dietary_restrictions[]`, `diet_animal_stance[]`/`diet_frameworks[]` (delta pass #12, untouched), the ingredient-swap feature itself (already exists, not redesigned here), the fork model, visibility, and `headcount`'s serving-size-scaling usage. No new entity, no new open questions. PRD.md, Design.md, `Prototype-Full.html`, `Prototype-Wireframe.html`, `Prototype.html` explicitly not touched — Design.md's Generate/S14 screen and `Prototype-Full.html`'s Generate/Adjust panel both still show dietary-mix UI that needs removing in a follow-up pass, queued as new item (p) in `00-Status.md`'s reconciliation debt list, folded into whenever the next `Prototype-Full.html` pass is dispatched. | Direct product-owner reasoning, reasoned through in detail: per-person dietary breakdown at plan-generation time is unrealistic — barely anyone has time to cook multiple different menus for different household members. When a real household has mixed dietary needs, what actually happens is one base meal with an ingredient substituted for whoever needs it (paneer instead of chicken, soy milk instead of dairy), not a separately-planned dish per person. That substitution need is already served by the existing ingredient-level swap-and-save feature on Recipe Detail (per-recipe ingredient editing, already part of this product) — it does not need a structural per-person planning concept layered on top of `GenerationInstance`. This removal is the deliberate answer to mixed-household dietary needs, not a gap. | solution-agent | claude-sonnet-5 |
